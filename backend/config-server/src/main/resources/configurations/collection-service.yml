server:
  port: 8030

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/collection
    username: restify
    password: restify
  jpa:
    hibernate:
      ddl-auto: update
      database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
application:
  config:
    file-url: http://localhost:8222/api/v1/file